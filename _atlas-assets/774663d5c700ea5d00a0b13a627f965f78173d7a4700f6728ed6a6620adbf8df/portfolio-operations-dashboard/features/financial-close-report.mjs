import {safeSpreadsheetCell} from './reforecast-report.mjs?v=f055ac3593292a5e';
import {financeSnapshot,lineageColumns} from './financial-snapshot.mjs?v=848d058bdec07b4e';
import {snapshotPdf} from './snapshot-pdf.mjs?v=e6a58eadc065a877';
import {readFinance} from './canonical-finance.mjs?v=b9e712aeb5970f5c';
import {readCanonicalBudgetVersion,assertBudgetReadback} from './canonical-budget-report.mjs?v=8bc8410839a1452a';
import {readRows} from './financial-close.mjs?v=2da82859f1969fa1';
import {loadXlsx} from './reforecast-intake.mjs?v=87e68da483f77228';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const freeze=o=>{if(o&&typeof o==='object'){Object.values(o).forEach(freeze);Object.freeze(o);}return o;};
export async function readCloseSnapshot(central,cid,period){
 const actor=central.getSession?.()?.user?.id,[record]=await readFinance(central,[cid],[period]);
 const projected=record?.summary?.close;if(!projected?.version_id||!record.publication_id)throw Error(record?.summary?.coverageReason||'No published full-month actuals are available for this period.');
 if(record.summary.actualCloseVersion!==projected.version_id)throw Error('Canonical close version mismatch. Refresh before exporting.');
 // The reporting projection deliberately carries only selected close fields. Read
 // lineage from the immutable close itself; a projected hash may be absent.
 const versions=await central.fetchJson(`/atlas_financial_close_versions?version_id=eq.${encodeURIComponent(projected.version_id)}&select=*&limit=1`),close=versions?.[0];
 if(central.getSession?.()?.user?.id!==actor)throw Error('Session changed while reading financial evidence.');
 if(!Array.isArray(versions)||versions.length!==1||close.version_id!==projected.version_id||close.community_id!==cid||close.period_key!==period||close.status!=='closed'||close.coverage!=='full_month')throw Error('Published close scope or status could not be verified. Refresh before exporting.');
 if(!/^[a-f0-9]{64}$/i.test(close.content_hash||'')||!Number.isInteger(close.row_count)||close.row_count<1)throw Error('Published close hash or row count is missing or invalid. Refresh before exporting.');
 if(['content_hash','community_id','period_key','source_hash','row_count'].some(key=>projected[key]!==undefined&&projected[key]!==close[key]))throw Error('Published close lineage does not match the immutable version. Refresh before exporting.');
 const rows=await readRows(central,close),budget=await readCanonicalBudgetVersion(central,record,cid,period);
 const [readback]=await readFinance(central,[cid],[period]);
 assertBudgetReadback(record,readback);
 if(record.summary.effectiveBaseline?.contentHash!==readback?.summary?.effectiveBaseline?.contentHash||record.summary.effectiveBaseline?.publicationId!==readback?.summary?.effectiveBaseline?.publicationId)throw Error('The effective baseline changed during readback. Refresh before exporting.');
 if(central.getSession?.()?.user?.id!==actor||readback?.publication_id!==record.publication_id||readback.summary?.actualCloseVersion!==close.version_id||readback.summary?.close?.version_id!==close.version_id)throw Error('The canonical version changed during the read. Refresh before exporting.');
 if(readback.summary.close.content_hash!==undefined&&readback.summary.close.content_hash!==close.content_hash)throw Error('Published close lineage does not match the immutable version. Refresh before exporting.');
 if(rows.some(r=>r.version_id!==close.version_id||r.community_id!==cid))throw Error('Canonical detail version or community mismatch.');
 return freeze(structuredClone({communityId:cid,period,versionId:close.version_id,contentHash:close.content_hash,sourceHash:close.source_hash,sourceFile:close.source_file,publicationId:record.publication_id,budgetVersion:record.summary.budgetVersion||null,status:'Canonically Published',financialSnapshot:record.summary.financialSnapshot||financeSnapshot(record.summary,record.publication_id),coverage:record.summary.coveragePolicy||null,carryIn:record.summary.carryInDisclosure||[],metrics:close.metrics,effectiveBaseline:record.summary.effectiveBaseline||{status:'unavailable',reason:'missing_baseline'},budget,rows}));
}
export function closeReportRows(s){
 if(new Set(s.rows.map(r=>r.gl_code)).size!==s.rows.length)throw Error('Canonical actuals contain duplicate GLs.');
 const active=new Map((s.effectiveBaseline?.status==='available'?s.effectiveBaseline.lines:[]).map(r=>[r.accountCode,r])),actuals=new Map(s.rows.map(r=>[r.gl_code,r])),budgets=new Map((s.budget?.rows||[]).map(r=>[r.gl_code,r])),gls=[...new Set([...actuals.keys(),...budgets.keys(),...active.keys()])];
 return gls.map(gl=>{const r=actuals.get(gl),b=budgets.get(gl),actual=r?.actual??null,budget=b?.budget??null,target=active.get(gl)?.amount??null;return {...lineageColumns(s.financialSnapshot),Community:s.communityId,Period:s.period,GL:gl,Account:r?.account_name||b?.account_name||gl,Actual:actual,Source_YTD:r?.ytd_actual??null,Original_budget:budget,Active_baseline:target,Actual_minus_active_baseline:actual===null||target===null?null:Math.round((actual-target)*100)/100,Baseline_type:s.effectiveBaseline?.sourceType||null,Baseline_version:s.effectiveBaseline?.versionId||null,Baseline_publication:s.effectiveBaseline?.publicationId||null,Baseline_content_hash:s.effectiveBaseline?.contentHash||null,Baseline_status:s.effectiveBaseline?.status||'unavailable',Actual_minus_budget:actual===null||budget===null?null:Math.round((actual-budget)*100)/100,Close_version:s.versionId,Content_hash:s.contentHash,Publication:s.publicationId,Budget_version:s.budgetVersion,Budget_content_hash:s.budget?.contentHash||null,Coverage_policy:s.coverage?.policyId||null,Carry_in_disclosure:JSON.stringify(s.carryIn||[]),Source_file:s.sourceFile,Source_hash:s.sourceHash,Source_location:JSON.stringify(r?.source_location??null),Budget_source_file:s.budget?.sourceFile||null,Budget_source_hash:s.budget?.sourceHash||null,Budget_source_location:JSON.stringify(b?.source_location??null)};});
}
export function closeReportHtml(s){const rows=closeReportRows(s),keys=['GL','Account','Actual','Original_budget','Active_baseline','Actual_minus_active_baseline','Source_YTD'];return `<h3>Published monthly actuals · ${esc(s.period)}</h3><p>Snapshot ${esc(s.financialSnapshot.fingerprint)}</p><details><summary>Source and version details</summary><p>${esc(s.sourceFile)}</p><p>Version ${esc(s.versionId)} · hash ${esc(s.contentHash)} · publication ${esc(s.publicationId)}</p><p>Approved original budget version ${esc(s.budget?.versionId||'Unavailable')} · hash ${esc(s.budget?.contentHash||'Unavailable')}</p></details><p>Variance is Actual − verified active baseline. The original approved budget remains a separate comparator. A missing account or amount remains unavailable.</p><p>Source YTD is supporting evidence and may include activity outside the selected full-month coverage.</p>${s.carryIn?.length?'<p>Carry-in disclosure: '+esc(s.carryIn.map(item=>item.reason+' (source periods '+item.sourcePeriods.join(', ')+'; '+item.treatment+')').join('; '))+'</p>':''}<div style="overflow:auto;max-height:600px"><table><thead><tr>${keys.map(k=>`<th>${esc(({Source_YTD:'Source YTD (supporting)',Original_budget:'Original budget',Active_baseline:'Active baseline',Actual_minus_active_baseline:'Actual − active baseline',Actual_minus_budget:'Actual − original budget'})[k]||k)}</th>`).join('')}</tr></thead><tbody>${rows.map(row=>`<tr>${keys.map(key=>`<td>${row[key]===null?'Unavailable':esc(row[key])}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`;}
export function closeReportCsv(s){const rows=closeReportRows(s),keys=Object.keys(rows[0]||{}),quote=v=>'"'+String(safeSpreadsheetCell(v)??'').replaceAll('"','""')+'"';return [keys,...rows.map(row=>keys.map(k=>row[k]))].map(row=>row.map(quote).join(',')).join('\r\n');}
export function closeReportWorkbook(s,XLSX){const workbook=XLSX.utils.book_new();XLSX.utils.book_append_sheet(workbook,XLSX.utils.json_to_sheet(closeReportRows(s).map(row=>Object.fromEntries(Object.entries(row).map(([key,value])=>[key,safeSpreadsheetCell(value)])))),'Canonical actuals');return workbook;}
export async function mountCloseReport(container,central,cid,period,{isCurrent=()=>true}={}){
 const snapshot=await readCloseSnapshot(central,cid,period);if(!container.isConnected||!isCurrent())return;
 container.innerHTML='<button data-export="csv">CSV</button><button data-export="xlsx">Excel</button><button data-export="pdf">Download PDF</button><p role="status"></p>'+closeReportHtml(snapshot);
 for(const button of container.querySelectorAll('[data-export]'))button.onclick=async()=>{button.disabled=true;try{const kind=button.dataset.export,name='ATLAS-closed-actuals-'+snapshot.period;if(kind==='xlsx'){const XLSX=await loadXlsx();XLSX.writeFile(closeReportWorkbook(snapshot,XLSX),name+'.xlsx');}else if(kind==='csv'){const url=URL.createObjectURL(new Blob([closeReportCsv(snapshot)],{type:'text/csv;charset=utf-8'})),a=document.createElement('a');a.href=url;a.download=name+'.csv';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}else{const url=URL.createObjectURL(new Blob([await closeReportPdf(snapshot)],{type:'application/pdf'})),a=document.createElement('a');a.href=url;a.download=name+'.pdf';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}}catch(error){container.querySelector('[role=status]').textContent=error.message;}finally{button.disabled=false;}};
 return snapshot;
}

export function closeReportPdf(snapshot){return snapshotPdf({title:'ATLAS Published Monthly Actuals',subtitle:snapshot.communityId+' / '+snapshot.period,snapshot:snapshot.financialSnapshot,rows:closeReportRows(snapshot),columns:[['GL','GL',58],['Account','Account',176],['Actual','Actual',90],['Original_budget','Original',90],['Active_baseline','Active baseline',90],['Actual_minus_active_baseline','Actual - active',110],['Source_YTD','Source YTD',100]]});}
