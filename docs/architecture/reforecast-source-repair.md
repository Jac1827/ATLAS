# Reliable reforecast evidence and separate STR publications

Workbook evidence and the reforecast intake envelope transfer as bounded chunks with SHA-256 manifests. Finalize validates the complete payload and inserts through the existing governed save functions in one transaction. Exact original bytes and serialized audit bytes remain readable. Request IDs bind retries to the same actor, scope and content. Diagnostics retain byte counts, duration, transport outcome and sanitized gateway correlation without retaining credentials or payload text.

Attempt diagnostics are excluded from immutable reference serialization. Retrying the audit-to-intake sequence after a lost begin or finalize response therefore preserves the same request manifest and returns the same saved import.

The governed source bundle includes every eligible close version through the requested horizon. This detects corrections to actual months outside the selected forecast range. Selected forecast cells overlapping governed actual months are rejected. Original approved budgets remain immutable comparators.

Verified local-image OOXML relationships are retained as evidence. An Excel error is classified as supporting media only when the entire local relationship resolves and the cell cannot feed financial authority. Missing relationships, financial selections and unresolved dependencies remain blocked; no cell address or numeric replacement is hard-coded.

RISE STR requires a separate scenario derived from an exact Conventional publication and original budget ancestry. Approved review evidence binds the full roster, availability and take-back months, occupancy, signed fees, rate method, application mode, reason and mapping version. Changes invalidate that review. Gross basis posts potential, negative vacancy and signed fees once; net basis posts net income once. Hello Landing GL 5144 requires explicit reviewed authority for any RISE posting.

Overlay publication never updates active operating baseline pointers. Reopening the overlay creates history within its own scenario. Paired exports use shared ancestry identifiers and distinct scenario publication identifiers, with a complete Conventional-to-STR bridge. PDF evidence attachments and XLSX roundtrips are checked against the same retained report rows.

Apply the additive governed-close, bounded-audit and STR-overlay migrations before publishing the corresponding client assets. Preserve older immutable audit references and publication history. Private source files, private financial amounts and production acceptance evidence belong outside Git. Synthetic or sibling-workbook test results do not establish approval of a missing original source.

Database changes were applied on September 24, 2026 as migrations `20260924232842`, `20260924232845`, and `20260924232853`. Post-migration hashes confirmed governed actual rows and the original approved budget unchanged. This schema release does not approve a source workbook or publish a business forecast.
