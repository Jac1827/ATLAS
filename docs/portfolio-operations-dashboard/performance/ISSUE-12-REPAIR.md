# Issue 12 performance repair — release evidence

Status: production acceptance is pending. GitHub issue #12 must remain open. Deployment success is not performance acceptance.

## Integrated follow-up and current production

The latest local runtime is `c586cbe22e1fc363458bf338be053d80ecb07185`, incorporating current main `c4cc985938b454dac82db2eef61e9e9cda7a095d` (PRs #34–#36). It preserves the reviewed save, market, governed reforecast and STR isolation repairs, all 81 migrations, and the closed current-client readiness gate. Performance PR #37 remains a draft; the earlier `435a478` timing matrix below is a distinct frozen checkpoint and must not be presented as the final integrated runtime's measurement.

Reports now omits 42 unused recommendation builds while preserving the other 28. Within one synchronous Reports render, exact argument tuples reuse read-only default records, staffing counts and quarter templates. The memo is discarded before control returns to the event loop; outside Reports, factories still return independent mutable results. Mutable per-evaluation Bonus roles and metrics are deliberately excluded. Eighteen full report payload comparisons across current, prior and sparse future periods passed mutation traps and source-refresh checks. The two short diagnostic runs retained all 11 canonical report/HTML/export parity checks. They used instrumentation and overlapped other test activity, so their timings are diagnostic only. [Default-record diagnosis](evidence/issue-12/reports-default-diagnostic.json), [staffing/template diagnosis](evidence/issue-12/reports-bonus-leaf-diagnostic.json).

A further read-only review found two discarded `detail` values in the year-over-year application summary. Removing those constructions leaves history, market fallback and coverage semantics intact. Eighteen full report/HTML/export comparisons and all 11 canonical parity checks passed; a focused regression test forbids rebuilding the unused details while asserting prior/future sparse history, zero/null presence and expected aggregates. In the diagnostic, recommendation calls fell from 28 to 14, with the remaining 14 preserved. The intermediate integrated `d0d2bbc` runtime passed 207/207 strict tests with zero skips and unchanged input fingerprint before this final removal. [Year-over-year diagnostic](evidence/issue-12/reports-yoy-diagnostic.json), [intermediate strict suite](evidence/issue-12/full-suite-runtime-d0d2bbc.json).

A post-recovery read at `2026-09-25T00:02:56Z` verified 81 migrations and the unchanged canonical parent version, effective timestamp and both hashes. Projection current/version/audit counts remained zero. This is a metadata check of the named source/projection, not proof of every operational row. [Preservation receipt](evidence/issue-12/source-preservation-after-recovery.json).

The publisher's sole `(jsonb)` overload, invoker security, reviewed body and declared 30-second setting are installed correctly. Available catalog/log evidence does not establish which effective timer was in force for the failed HTTP calls. PostgREST 14.5 source supports function-level hoisting; the process allowlist, pre-request hooks and cached routine settings are not visible through enabled project tools. No speculative global timeout or index change was applied, and no further publication attempt is queued. A read-only operator check of those specific runtime settings is the next way to prove a configuration defect. The pending Nano-to-Micro resource option is not a proven cure. [Investigation and conditional remedies](PUBLISHER-TIMEOUT-INVESTIGATION.md).

Final integrated runtime `c586cbe22e1fc363458bf338be053d80ecb07185` passed **208/208 strict tests**, zero failures/skips, with unchanged executable-input fingerprint `0b2a296e210d087dad55894a0dccd282e6e96a4553db2ea804dc64f94f40d62f`. Both private budget/delinquency fixtures ran; the separately optional private utility workbook remains unavailable. This result includes all 81 migration replays and the source/auth/import/reporting/forecast/rollback checks discovered by the runner. [Exact final-runtime receipt](evidence/issue-12/full-suite-runtime-c586cbe.json).

The subsequent stable publication [run 36075816912](https://github.com/Jac1827/ATLAS/actions/runs/36075816912) selected exact client `2fab61de2627b7c8753f2ca8ebe7b0b879a67d25`, release `d1905d842d9fb2f80f4b12d6a6484c05dc9b1afdb1c2f9c80ed7372a3d8bee3d`. Worker `d160e697-510a-4746-8b07-e54b569b9b5d` is active at 100%; Pages published the same artifact at `2026-09-25T00:08:29Z`. Independent verification found matching index/manifest bytes and all 15 checked immutable assets on both hosts, with both earlier releases retained in `5528ef8c869147b1f62ed3db3fb39b846ec9ad8f`. This stable client incorporates the coordinated save/reforecast repairs and retains the older startup architecture. Its production workflow ran at `8e1b30fc`: 193 passed, zero failed, two private-fixture skips out of 195; that is separate from the PR #36 preview's 198/200 and local strict 200/200 results. The Doro coordinator's fresh signed-in check opened Home, Budget Builder, governed Reforecast, Reports, Data Import, Community Command and Bonus with 14 authorized active scopes and no console errors/warnings. It performed no financial writes, approvals or exports. Functional navigation is not performance or report-value acceptance. [Deployment and functional receipt](evidence/issue-12/stable-production-release.json).

After the stable signed-in check, a final metadata-only read at `2026-09-25T00:14:26Z` again verified the unchanged parent version/time/hashes, all 81 migrations and zero derived current/version/audit rows. The publisher body and declared settings were unchanged; runtime REST settings remain unobservable. [Post-publication preservation receipt](evidence/issue-12/source-preservation-after-stable-release.json).

Current stable-client rollback reference: source `2fab61de2627b7c8753f2ca8ebe7b0b879a67d25` and release `d1905d842d9fb2f80f4b12d6a6484c05dc9b1afdb1c2f9c80ed7372a3d8bee3d`, preserving the reviewed market/save/reforecast changes. The earlier `3d80173` recovery reference remains retained for incident forensics; choosing it now would also remove those newer client fixes. Keep backend migrations, source data and append-only retained releases intact.

## Final integrated candidate measurements

Exact archived runtime `c586cbe22e1fc363458bf338be053d80ecb07185` was measured separately after the coordinated heavy test jobs stopped: 20 startup samples (five cold and five warm per device), 110 navigation observations (first plus ten repeated loops across five workspaces), and four slow/unavailable-source variants. It used the same retained fixture and unchanged runner as the earlier three-build matrix. No page/render errors or external network responses were observed; all blocked writes were synthetic live-presence calls (132 updates and 20 endings), with zero operational write attempts. All 20 startups rendered once; cold samples had zero CDP cache hits and warm samples had 43. Reports had two legitimate preview-cache misses and nine hits per device.

Recorded med/max milliseconds. Prior435-run groups retain their own contention limitations; c586 was measured separately.

| Revision / device | Cold | Warm | Repeated Reports | Repeated Reports max task |
|---|---:|---:|---:|---:|
| 4263a58 historical / desktop | 783 / 843 | 725 / 1209 | 924.5 / 1026 | 934 |
| 4263a58 historical / mobile | 1991 / 2066 | 1957 / 2182 | 3744 / 3907 | 3489 |
| 3d80173 baseline / desktop | 1678 / 1796 | 1541 / 1589 | 1084.5 / 1153 | 1057 |
| 3d80173 baseline / mobile | 3466 / 3591 | 2936 / 3604 | 4458 / 4775 | 4231 |
| 435a478 checkpoint / desktop | 470 / 515 | 472 / 496 | 74 / 150 | 103 |
| 435a478 checkpoint / mobile | 1646 / 1697 | 1653 / 1670 | 283 / 590 | 422 |
| c586cbe candidate / desktop | 499 / 559 | 441 / 473 | 72.5 / 125 | 78 |
| c586cbe candidate / mobile | 1520 / 1655 | 1503 / 1566 | 250 / 467 | 303 |

All measured desktop timing checks passed. **Seven mobile checks still failed** under the unchanged budgets: cold-start longest task 207/200 ms; first Home navigation 1,066/1,000 ms and task 217/200 ms; first Reports task 362/200 ms; repeated Reports navigation 467/400 ms and task 303/200 ms; first Command task 214/200 ms. These are observed maxima, not percentiles, and no sample was discarded. Repeated Reports still had one legitimate fresh-input rebuild; it must not be excluded to make the budget pass. Further bounded rendering work is required, alongside the blocked production acceptance. Staging work must preserve exact source/actor/scope checks through every yield, including A→B→A changes; simply deferring an iframe or dropping key dependencies would not resolve the measured data-builder cost safely.

Post-GC heap growth was 3.44 MiB desktop / 0.69 MiB mobile; sampled maxima were 134.39 / 140.11 MiB. These checkpoints exclude worker heaps and do not establish transient peak memory. A separate post-measurement same-origin probe passed all 11 baseline/current retained-value, source, report, exact-HTML and export-row checks. It fixes the generation clock and shares one origin because the document embeds that origin in its logo URL. Its timing samples are excluded from acceptance. No observed difference was introduced by the integrated save/reforecast fixes in these probes; empty synthetic finance RPCs still prevent any claim about live finance response parity.

The historical/current-head controls above are the original immutable samples, with their previously reported test contention. They were not rerun under the final candidate's quieter conditions. Existing user browser tabs and OS processes remained uncontrolled during the new run. These comparisons therefore do not prove a clean causal speedup under identical machine load. They also do not replace authenticated production, empty-cache first-user, original 29-batch browser-profile, live session refresh, two-session report/export or transient-peak acceptance.

[Final sanitized request/stage observations](evidence/issue-12/replay-c586cbe.json), [summary](evidence/issue-12/replay-c586cbe-summary.json), [all seven failures](evidence/issue-12/replay-c586cbe-budget-misses.json), [same-origin parity](evidence/issue-12/replay-c586cbe-parity.json), and [Reports stage evidence](evidence/issue-12/replay-c586cbe-reports-stages.json). Runtime source, fixture and runner fingerprints accompany the evidence; private raw source files and displayed operational values are omitted from the public timing package. The earlier public 435 artifacts likewise omit the displayed operating value; their private raw observations, timing samples, hashes and comparison conclusions are unchanged.

## Reproducible controls

The signed-in production baseline was captured before implementation on `3d80173de32f227410663662a523932c50ecaa2b`, which had superseded the request's `850295533548d7adb2371d69cd0f30a95ef6753d`. Historical control: `4263a58cca7319d8d69563bc9e91e7bf6e64a3a5`. The baseline initial HTML was 3,384,230 bytes. The candidate separates its roughly 14 KB document, core JavaScript/CSS and lazy workspace modules. This reduces initial HTML parsing and enables immutable asset caching; it does not imply that all cold-start JavaScript bytes disappeared.

Two real signed-in profiles had different retained state. The original browser had 29 import batches and 14 active communities. The independently signed-in tracing profile had no local history and legacy seeded data. The canonical central archive is version 1, effective `2026-09-19T02:36:25.886387+00:00`, SHA-256 `44fff7341af4fe69f97dffe49736f7d6b81c1c465a59ac1477cdc745443d261f`: 13 batches, 21 sources and 28 community records. None of these volumes may be silently substituted for another. Existing browser storage and central source records are retained. No local operational state is automatically published to reconcile this difference.

### Signed-in baseline observations

These are single observations, not five-run statistical results. A loading card's early paint was not treated as a usable workspace.

| Tracing profile | Desktop | Mobile emulation |
| --- | ---: | ---: |
| Cold usable workspace | 32,644 ms | 55,578 ms |
| Warm usable workspace | 46,160 ms | 84,221 ms |
| First Reports navigation | 1,518 ms | 4,925 ms |
| Longest navigation task | 1,552 ms | 4,965 ms |

Desktop: 1440×900, DPR1, unthrottled CPU. Mobile: 390×844, DPR3, 4× CPU slowdown. Network was not artificially throttled for these rows. The original browser's different local state showed 1,229 ms import hydration, six startup Home renders and a 5,810 ms Bonus long task; it is a separate baseline. Complete request/stage/heap records are retained privately under ignored `output/issue12-baseline/` and do not contain credentials or response bodies. The 1.8 ms session probe had a valid token and was not a forced network refresh. An actual refresh-token request was subsequently observed at `2026-09-24T21:23:51.019Z`, taking 7,068.6 ms during the service incident; that is a separate observation, not an uncontended baseline. Trace-file export was unavailable; browser measures and sanitized request records were retained.

## Acceptance budgets

| Metric | Desktop | Mobile emulation |
| --- | ---: | ---: |
| Warm authenticated shell | 300 ms | 600 ms |
| Warm usable workspace | 1,000 ms | 2,000 ms |
| Cold HTTP-cache usable workspace | 3,000 ms | 5,000 ms |
| First lazy navigation | 500 ms | 1,000 ms |
| Repeated navigation | 200 ms | 400 ms |
| Longest main-thread task | 100 ms | 200 ms |
| Startup blocking time, first 10 seconds | 300 ms | 600 ms |
| Steady / peak main-thread heap | 150 / 250 MiB | 150 / 250 MiB |
| Heap growth after 10 equivalent navigation loops | 10 MiB | 10 MiB |
| 4,822,131-byte workbook preview | 1,000 ms | 2,000 ms |
| Workbook cancellation cleanup | 200 ms | 400 ms |

Use five cold/five warm starts and ten navigation loops with identical authorized scope, reporting period, archive/publication references and volume. Record all samples, median and maximum; tiny samples are not reliable percentiles. Also cover slow network, unavailable source, small/large histories, token refresh, actor/scope/access loss, cancellation and two independently authenticated sessions. Read-only local replay of historical builds is labeled replay, not live acceptance. Preserve report/export values, version references, source lineage and zero/null distinctions.

## Controlled three-build comparison

The complete identical-data replay compares historical `4263a58`, baseline `3d80173` and candidate `435a478601f89a9c4028de8104c9e9e5cf74cf1b`: 60 startups, 330 navigations and 12 slow/unavailable-source variants. Each build/device uses five cold/five true HTTP-warm loads, followed by first navigation and ten repeated loops. Cold samples have zero CDP cache hits; warm samples show real asset reuse. The same authorized operating data is preseeded, so cold describes HTTP asset cache, not an empty new user's local database. These are local replay measurements, not authenticated production acceptance. See [raw sanitized observations](evidence/issue-12/replay-435a478.json), [complete summary](evidence/issue-12/replay-435a478-summary.json), and [reproduction instructions](../../../tools/performance/WORKSPACE-REPLAY.md).

| Build / device | Cold usable median / max | Warm usable median / max | Repeated Reports median / max |
| --- | ---: | ---: | ---: |
| Historical / desktop | 783 / 843 ms | 725 / 1,209 ms | 925 / 1,026 ms |
| Historical / mobile | 1,991 / 2,066 ms | 1,957 / 2,182 ms | 3,744 / 3,907 ms |
| Baseline / desktop | 1,678 / 1,796 ms | 1,541 / 1,589 ms | 1,085 / 1,153 ms |
| Baseline / mobile | 3,466 / 3,591 ms | 2,936 / 3,604 ms | 4,458 / 4,775 ms |
| Candidate 435a478 / desktop | 470 / 515 ms | 472 / 496 ms | 74 / 150 ms |
| Candidate 435a478 / mobile | 1,646 / 1,697 ms | 1,653 / 1,670 ms | 283 / 590 ms |

The candidate meets warm/cold usable budgets but retains [ten budget misses](evidence/issue-12/replay-435a478-budget-misses.json): desktop Reports tasks of 105/103 ms; mobile startup tasks up to 214 ms; first Home 1,064 ms with a 205 ms task; Reports first/repeated tasks of 457/422 ms and a repeated navigation of 590 ms; and first Command's 210 ms task. Reports had no API requests in those slow navigation intervals; recorded synchronous render stages were 456.5 and 398 ms. Subfunction attribution requires further profiling. Later mobile Reports navigations were 269–294 ms. These misses are retained without changing the budgets; production acceptance has not passed.

All eleven baseline/candidate value, source-normalization, report-data/HTML, aggregate and export-row checks passed, with no JavaScript/render errors, external responses or candidate operational-write attempts. Heap growth after ten loops was 0.60/0.72 MiB (desktop/mobile); largest sampled heaps were 140/124 MiB. These are checkpoint samples, not transient peak measurements.

Other tasks ran local tests during parts of the controls: one suite ended at 23:36:33.259 UTC, another ran 23:36:56.080–23:38:02.779, and focused tests ran approximately 23:38–23:40. A recovery browser tab may also have polled until 23:36. Other heavy work paused before candidate sampling, but original user tabs and lightweight recovery work remained. These changing contention conditions limit causal speedup claims; this was not an idle-machine comparison. Exact observations and limitations are retained in the evidence.

Operating-data, portfolio-aggregate and export-row probes match between the controls. Community Progress changed before this repair: 72 differing paths reflect reconciled stabilization (41), guest-card trends (8), lead-source/traffic availability and totals (12), and derived alerts/lists (11). No move-in target values differ for this fixture. [Anonymous field differences](evidence/issue-12/historical-report-differences.json) retain paths/types without community names or private values. The candidate must match the current baseline's report values and markup exactly; the historical report is not falsely labeled identical. The historical build also lacks the later Home-history diagnostic API, so that optional probe compares baseline and candidate only.

## Changes and safeguards

- `workspace-core.js`, `atlas-core.css`, `features/*-workspace.js`, `performance/feature-loader.js`: external core and lazy Reports, Data Import, Bonus and administration; spreadsheet/PDF/presentation/map/forecast dependencies load through their feature boundary. Stable mounts are retained only for matching actor, authorization, community, period and module context.
- Home history preparation yields between bounded community/month groups on cache misses. A loading view remains explicitly busy until the real workspace is ready; navigation, authorization and request-epoch changes reject stale completions. Failed startup preparation retries the full initializer, including when a previously ready session is reinitializing. Metadata-only preparation omits unused summaries and recommendations. Reports reuse access checks, source normalization and pure calculations only within one synchronous render, and compare exact current inputs before reusing a retained preview. Null, zero, undefined, sparse arrays and source lineage remain distinct; these caches never survive an authorization-scope mismatch.
- `features/workspace-bootstrap.mjs`, `workspace-projection-worker.js`, `workspace-integrity-worker.js`, `workspace-canonical.mjs`, `workspace-publication.mjs`: source-bound derived read model, freshly verified access namespace, visible effective/verification timestamps, cached first usable render, independent optional reads after paint, stale-response guards and honest projection-only publication retries. Large projection integrity checks run in an abortable worker. Missing scoped data never falls back to an unfiltered archive; a missing central client in a hosted deployment also fails closed.
- `features/import-history*.mjs`, `occupancy-replay-browser.js`: immutable split evidence, lightweight current views, paged history, hash-verified snapshot references, atomic revision checks and cancellation. Explicit archive/export/rollback actions retain full evidence.
- `features/canonical-finance.mjs`, `financial-close.mjs`, finance migration: request only the selected authorized communities/periods and reuse per-request finance context. Publication reads remain fresh; no source formula or financial actual is changed. Existing measured indexes remain unchanged.
- `centralization/atlas-central-client.js`: bounded/cancellable reads, actor/access guards and concurrent independent access reads. Rendering no longer publishes property graph or workspace changes. Same-actor token rotation does not clear valid caches; actual access changes do. People, goals, notifications, presence, administrator views, financial broadcasts and restore entry points reject old completions after an actor, access, backend, database or session-epoch change. Accepted atomic writes settle in their original context. Marketing metadata reads use verified community relationships and cannot use a matching name to override a conflicting canonical ID.
- `tools/package-atlas-assets.mjs`, `build-atlas-site.mjs`, Worker and deployment workflow: content-addressed complete asset trees, verified append-only retention branch and one artifact for both hosts. Old open pages retain exact dependency URLs. Capacity overflow fails without pruning.

The scoped reader mirrors existing role, community and module authorization. Raw archive RLS is unchanged. Operational data is filtered by field for limited modules; separate-API modules get identity only; self-service Settings can open after access verification when operational data is unavailable.

## Integrity and query evidence

All 102 immutable archive part hashes, the 39,781,393-byte ZIP hash and all 28 member hashes were verified. An isolated local worker restored all 27 retained records, migrated the 399 MB uncompressed archive, exported every collection, reimported into a second isolated database and restored all 13 before-snapshots exactly. Source bytes and snapshot hashes remained unchanged. Migration took 3.55 seconds; compact history was 395,111 bytes; main-page observed heap peak was 1.55 MB (worker memory excluded). Cancellation completed in 11.7 ms without changing the revision. These are local integrity/scaling checks, not production startup claims.

See [finance query plans and parity](FINANCE-REQUEST-PERFORMANCE.md), [scoped reader authorization](SCOPED-WORKSPACE-PROJECTION.md), and [asset retention/rollback design](IMMUTABLE-ASSET-PACKAGING.md). No index or database resource setting was changed based on speculation.

## Production service incident during verification

The 21:00–21:13 UTC diagnostic window on September 24 showed 19 PostgREST PGRST003 internal connection-pool acquisition timeouts. Successful profile/community/document reads averaged approximately 40–50 seconds, with some requests timing out around 126 seconds. Auth health remained responsive. A database snapshot showed no blocked sessions or deadlocks and connections below the configured maximum. Dashboard swap and memory pressure support a contributing resource-pressure hypothesis; they do not establish a precise CPU/root-cause claim. A controlled project-service restart was initiated after publication verification was blocked. No plan, permission or data reset was performed. A small REST read recovered to 0.35 seconds. Metadata readback then confirmed that the uncertain derived write was absent. One guarded retry also timed out, and a second metadata read confirmed absence. The exact 6,999,497-byte projection saves through the same function on the full local schema in 362 ms with current, immutable version and audit rows retained. The live function has no user triggers and uses the existing unique-key index; that write evidence alone does not justify changing its query strategy or indexes. Supabase shows Micro as a free upgrade from Nano at the same $0.01344/hour; the requested compute choice is still pending.

After database deployment, metadata reads recovered to 110.5 ms on average (15 observations, maximum 180 ms), and no blocked or idle-in-transaction sessions remained in the later snapshot. One guarded materialization attempt at 21:55:48 UTC still failed in 10.587 seconds; readback again confirmed no derived document and an unchanged parent. The database cancellation occurred inside the immutable-version insert, after reaching the existing authenticated role's eight-second statement deadline. This is distinct from the earlier pool-acquisition incident. The cutoff covers cumulative work in the transaction, so it does not prove that the insert alone consumed eight seconds or establish whether CPU, compression or I/O caused the overrun. No index, global deadline, or resource setting was changed on that evidence.

A narrowly scoped, administrator-only publisher was subsequently deployed in PR #31. It retains the existing audited writer, locks and validates the exact source parent, permits only the fixed derived key, and declares an outer 30-second deadline. General authenticated requests remain at eight seconds. The first dedicated HTTP attempt started at `22:13:36.896Z` and failed after 59,243 ms; the database logged cancellation at `22:14:27.161Z` during the serialized size check, before the parent lock or any write. HTTP elapsed time does not reveal the effective database deadline or divide queueing, body parsing, execution and response transport. Unrelated reads and finance calls were also slow in the same window. Readback at `22:16:44.314Z` confirmed zero derived current/version/audit records and an unchanged source parent.

The `22:55–23:06 UTC` health window showed improved small reads: 108 document reads averaged 326 ms, and a metadata snapshot showed 15 client connections against a maximum of 60, with no blockers or idle transactions. Finance still had five timeouts near eight seconds. After this measured change in service conditions, one guarded publication attempt began at `23:08:28.241Z` and failed after 76,576 ms. The database cancellation at `23:09:31.031Z` was at publisher line 84, the call to the existing document writer; it did not expose a nested hashing, storage, version or audit operation. The gateway recorded 46,651 ms for its origin span, which is not the same span as browser elapsed time. Readback at `23:11:13.883Z` confirmed **zero derived current, version and audit rows**, with the parent version, effective timestamp, stored payload hash and archive hash exactly unchanged. The failed job has stopped; no database write or retry remains pending. Source and evidence are intact. No global timeout or compute change has been made, and no further publication attempt is queued. These observations do not establish a CPU, memory or I/O root cause or prove that another large write will succeed.

## Generated workbook comparison

The same 4,822,131-byte generated workbook was checked five times per build/device. The worker preview preserves 50,001 unrelated source rows while transferring an 80-row sample, and retains all 220 rows of the multi-section report, including its late section. Candidate desktop previews took 143–170 ms; mobile-emulation observations took 245–315 ms. In-flight cancellation completed within 0.1 ms in the candidate, and no main-page long tasks were observed. Both control builds passed identical value checks. These local worker measurements do not include authenticated import publication, production network latency, or worker heap. The historical CDN parser was served with byte-identical pinned content; request routing disabled HTTP cache, and CPU emulation applied to the main target. Reproduce with `tools/performance/workbook-browser-benchmark.mjs`; raw observations remain under ignored `output/issue12-workbook/`.

## Release and rollback gates

The frozen replay candidate passed all 197 discovered tests with zero failures/skips and unchanged fingerprint `6fd2836ca1ef597922daa3f5c7faf227033bc0e47c3fff6d57ac9f1956ad8369`, exact commit `435a478601f89a9c4028de8104c9e9e5cf74cf1b` ([receipt](evidence/issue-12/full-suite-runtime-435a478.json)). Product code is the Home/Reports revision `2251388d812ca5bca26c2271ecb3284613b4487e`. Integrating the reviewed recovery controls then passed **198/198 strict tests**, zero failures/skips, at `a631cb36aa267407d13f3e02b323d2f7fb5f0d9f`, with unchanged fingerprint `e7d3fb460f0c3cd59b5fb90c749bbd5469a62c91aeef15a52a972f5e2f213cc3`. This includes startup/navigation preparation, failure/retry recovery, same-actor scope changes, late-completion rejection, restore guards and exact client recovery packaging. Both supplied private budget/delinquency fixtures were exercised. An optional private utility-workbook branch remains unverified when its separate fixture directory is absent. Earlier isolated fixture setup omissions and a fixed-port collision were corrected without weakening assertions. The deployment workflow reruns the discovered suite and explicitly reports private-fixture skips. [Sanitized recovery-integration test results](evidence/issue-12/full-suite-integration-a631cb3.json) contain no private paths or payloads. Published evidence is excluded from the executable-input fingerprint to avoid self-reference. Detailed logs remain private under ignored `output/issue12-repaired/full-suite-8/`.

Authenticated production acceptance, two independent live report/export comparisons, and final before/after production timings remain release requirements. The replay supplies empty finance RPC responses: its operating-data/report-render parity does not prove live selected-community/period finance requests or current publication response parity. Canonical 13-batch rollback evidence and larger synthetic histories do not establish migration of the distinct original 29-batch browser profile. Heap checks sample after startup and navigation, so their maximum is a sampled maximum, not a measured transient peak; the 250 MiB peak budget remains unproven. Final-client token-refresh behavior is covered by synthetic integration tests; the real network refresh observation belongs to the earlier service incident. Publish these limits, final run results and deployment IDs with issue #12; keep it open until the live requirements pass.

Database preparation was merged in [PR #30](https://github.com/Jac1827/ATLAS/pull/30), merge `0c1ac0e584419e721d8ec70f899436738a3d1585`, after all five applicable checks passed. The configured integration automatically applied `20260924203341_finance_request_context.sql` and `20260924205459_scoped_workspace_projection.sql`. Thirteen affected function bodies match the reviewed SQL. Private helper grants remain closed, public readers remain authenticated-only, and the anonymous production RPC probe is denied.

[PR #31](https://github.com/Jac1827/ATLAS/pull/31), merge `9ef02ff08e3c424be52d6bc3d05f90559dbbad37`, automatically deployed `20260924220418_workspace_projection_publisher.sql`. Production history contained all 77 migrations at that verification; the publisher body, invoker security, execution grants and local settings matched the reviewed migration. Main's five applicable checks passed; the branch-preview job was correctly skipped. No manual migration raced the integration. A concurrent Doro repair subsequently reported applying `20260924232842`, `20260924232845` and `20260924232853`; client recovery must preserve those migrations too. See [publisher transaction guarantees](WORKSPACE-PROJECTION-PUBLISHER.md).

The earlier `22:24:35 UTC` observation found both hosts serving the prior 3,384,230-byte interface, SHA-256 `c1fde41a7d1cdcdddd71a6e21aff1d8d7ca47b06d7b9bea06a98891ff918525a`. Native Cloudflare Git Builds had been disconnected and GitHub Pages switched to the Actions deployment path so packaged releases have one publisher. Those configuration changes did not themselves activate the candidate; the later PR #32 merge and production deployment did.

At `2026-09-24T22:24:35Z`, the Worker was 100% version `dd5ebd5d-24f5-443c-96a1-87c81353ff8d`, deployment `62c33985-66e1-4600-ba1d-e9f7c08a1a69`, from [PR #31's production job](https://github.com/Jac1827/ATLAS/actions/runs/36065959088/job/107855653315). Pages still served [PR #30's deployment](https://github.com/Jac1827/ATLAS/actions/runs/36064320062/job/107850450250); PR #31 did not redeploy Pages after the source-mode switch. Exact HTML equality confirmed alignment at that historical check despite the different backend-only release heads. The production publisher is serialized across all branches, including manually dispatched production runs.

The [inactive preview job](https://github.com/Jac1827/ATLAS/actions/runs/36070906618/job/107871377759) for `a31c7e8e46fd847e510dadac1e74e02ef322d8cd` succeeded at `23:08 UTC`, uploading Worker version `9fa702d8-cd07-459d-a2d4-f6566156d7e7` and packaged release `75f860f6da674427159a5ba8c5c2ecb0d7e208c49a35d168dcd16877dcf6a370`. Explicit selection of the verified Cloudflare account resolved the earlier account-discovery failure without changing the deployment credential or its permissions. This job uses `versions upload`, not production deployment; the production job was skipped and Pages was not published. It proves upload of that candidate revision only, not authenticated production acceptance or subsequent working-tree changes.

**Production incident:** [PR #32](https://github.com/Jac1827/ATLAS/pull/32), candidate `a31c7e8e46fd847e510dadac1e74e02ef322d8cd`, merged while acceptance was pending at `23:19:51 UTC`, creating main `0ae43c47d3a216e480d01abf14036e97cb2dcd70`. [Production job 107875394264](https://github.com/Jac1827/ATLAS/actions/runs/36072173981/job/107875394264) successfully deployed both hosts at `23:22 UTC`. Worker version `9f1a463a-bca9-4b3c-82b1-725e8b81c197` became active at 100%, deployment `452a2850-81ca-4bf8-a45b-b1474e7f11aa`; Pages received the same packaged release `75f860f6da674427159a5ba8c5c2ecb0d7e208c49a35d168dcd16877dcf6a370`, retained by commit `09f271122e2436bc642deddabe1dfc7f8cecf93e`. The earlier inactive preview status does not describe this later deployment.

At `23:28:13.959 UTC`, a bounded read-only check still found zero rows for the exact required projection and its versions, and zero projection audit rows. The parent version, timestamp and both hashes remained unchanged. Project status was `ACTIVE_HEALTHY`; that status did not establish application readiness. A fresh signed-in Pages session at approximately `23:30 UTC` displayed **“Workspace source unavailable” / “Source projection is not ready.”** Functional startup was blocked at that observation. The existing user Budget tab was left untouched.

The earlier production workflow checked tests, endpoint availability/anonymous denial and retained asset integrity, but did **not** check that the required projection existed. Those checks passed without preventing the activation. [PR #33](https://github.com/Jac1827/ATLAS/pull/33), merged as `71a5d69407cf8f2ec117f512d6e70d72830f1842`, now blocks normal current-client activation unless `ATLAS_SCOPED_WORKSPACE_READY` is explicitly true. The repository variable was verified unset; the merge's automatic production job correctly skipped. A reviewed manual recovery requires the full source commit and independently established release hash, rejects mismatches before retention publication, and preserves all retained releases and current backend/Worker settings.

**Verified client recovery:** [manual run 36073964558](https://github.com/Jac1827/ATLAS/actions/runs/36073964558/job/107880936140) deployed exact prior client `3d80173de32f227410663662a523932c50ecaa2b`, release `92989f640671df582527300a54b79cc80d46f0c4300564528c333d72cdbae540`. At that recovery, Worker version `e0d8029c-3be0-413e-9743-465f0c8aa1ea` was 100%, deployment `af28c476-fff7-47ca-a126-208371f99e4b`; Pages deployment is `71a5d69407cf8f2ec117f512d6e70d72830f1842`. Both hosts then served identical packaged HTML (3,384,460 bytes, SHA-256 `486019decee3b96dfb64ab880113ebdce958abcd99fbede08be6f1c45f195faa`) and the identical manifest `42c52b6607fe67f4a2ddf21269820c123c706c57b881f74bebf1e473cb1c2796`. Both retained namespaces remain available. The packaged HTML differs from original source only as recorded by the asset packager, including its immutable base URL.

The recovery job passed 191 tests with zero failures and two explicit private-fixture skips; that older current-checkout suite is separate from the 198 strict local candidate checks. Metadata at 23:47:05 UTC confirmed all 80 migrations then present (including the three concurrent Doro migrations), unchanged publisher/source hashes, and zero projection current/version/audit rows. A separate signed-in administrator session then opened Home, Reports, Data Import, Community Command and Bonus with 14 authorized communities. Reports and Bonus retained the prior client's delays, and two Command fields showed unavailable effective baselines. This verifies access restoration, not repaired-client performance or full finance acceptance. No save, import, approval, payment or export action was performed, and the original Budget Builder tab was untouched. See the [recovery and preservation receipt](evidence/issue-12/client-recovery.json).

No projection publication retry, global timeout change or compute change is queued. Later current-client activation requires exact projection readback and authenticated functional/value/performance acceptance; issue #12 remains open. Other coordinated source repairs and their subsequent migrations must be preserved when integrating this candidate.

Published evidence: [signed-in baseline](evidence/issue-12/signed-in-baseline.json), [generated workbook comparison](evidence/issue-12/workbook-comparison.json), [database and hosting release](evidence/issue-12/backend-release.json), [affected files](evidence/issue-12/affected-files.json), and [full test results](evidence/issue-12/full-suite.json). Baseline request paths omit query values; response bodies, headers, credentials and private source files are excluded. Overlapping startup/navigation observation windows must not be summed.

Earlier incident rollback reference: `3d80173de32f227410663662a523932c50ecaa2b` (the current stable reference above also preserves the later coordinated client fixes). Repackage that source while retaining every tree on `atlas-asset-releases`; do not deploy an empty retention directory. `centralization/finance-request-context.rollback.sql` contains the prior finance reader definitions for a reviewed forward rollback migration. Preserve all archives, derived documents, audit rows and version history. Keep new scoped endpoints available for already-open repaired pages unless a separate security incident requires revocation. A rollback must not modify operational source records.
