// Local read-only replay. Fixture files and results stay in ignored output/.
import os from 'node:os';import fs from 'node:fs';import http from 'node:http';import path from 'node:path';import crypto from 'node:crypto';import {createRequire} from 'node:module';
import {pathToFileURL} from 'node:url';
if(process.argv.includes('--help')||!process.argv[2]){console.log('Usage: node tools/performance/workspace-replay-benchmark.mjs CONFIG.json');process.exit(process.argv.includes('--help')?0:1);}
const config=JSON.parse(fs.readFileSync(path.resolve(process.argv[2]),'utf8'));
for(const key of ['fixtureDirectory','outputDirectory','builds'])if(!config[key])throw Error('Missing replay config: '+key);
const require=createRequire(import.meta.url),{chromium}=require(process.env.ATLAS_PLAYWRIGHT||'playwright');
const root=process.cwd(),out=path.resolve(config.outputDirectory),fixture=path.resolve(config.fixtureDirectory);fs.mkdirSync(out,{recursive:true});
const roots=Object.fromEntries(Object.entries(config.builds).map(([name,build])=>[name,path.resolve(build.assetRoot)]));
for(const name of ['historical','baseline','repaired'])if(!roots[name]||!fs.existsSync(roots[name]+'/index.html'))throw Error('Missing frozen build: '+name);
const {accessNamespace}=await import(pathToFileURL(roots.repaired+'/features/workspace-bootstrap.mjs').href);
const period=config.reportingPeriod||'2026-09';if(!/^\d{4}-\d{2}$/.test(period))throw Error('Invalid reportingPeriod');
const reportYear=Number(period.slice(0,4)),reportMonth=Number(period.slice(5))-1;
const projection=JSON.parse(fs.readFileSync(fixture+'/startup-projection.json')),bundle=JSON.parse(fs.readFileSync(fixture+'/bundle.json')),parent=JSON.parse(fs.readFileSync(fixture+'/parent.json')),summary=JSON.parse(fs.readFileSync(fixture+'/projection-summary.json'));
const actor=crypto.randomUUID(),backend='https://replay.invalid',syntheticAuth={accessToken:crypto.randomBytes(32).toString('base64url'),refreshToken:crypto.randomBytes(32).toString('base64url')};
const roster=Object.entries(projection.communityData).map(([name,r],i)=>({community_id:r.communityId||`22222222-2222-4222-8222-${String(i+1).padStart(12,'0')}`,display_name:name,canonical_name:name,status:r.communityStatus||'active',market:r.market||'',regional_grouping:r.regionalGrouping||''}));
const profile={user_id:actor,email:'replay@risere.com',display_name:'Synthetic Replay Admin',role:'admin',status:'active',account_status:'active',allowed_community_ids:[],allowed_market_values:[],allowed_region_values:[],locked_tab_ids:[],locked_page_keys:[],bonus_permissions:[],community_access_records:roster,access_backend:backend,access_verified_at:'2026-09-24T12:00:00Z'};
const namespace=await accessNamespace({getSession:()=>({user:{id:actor}}),getConfig:()=>({supabaseUrl:backend})},profile);
const binding={scopeFingerprint:'c'.repeat(64),projectionVersion:1,projectionContentHash:'d'.repeat(64),projectionHashFormat:'postgres-jsonb-sha256',fullProjection:true};

const sha=value=>crypto.createHash('sha256').update(value).digest('hex');
function assetManifest(base){const result={};const walk=dir=>{for(const entry of fs.readdirSync(dir,{withFileTypes:true})){const file=path.join(dir,entry.name);if(entry.isDirectory())walk(file);else if(entry.isFile())result[path.relative(base,file)]={bytes:fs.statSync(file).size,sha256:sha(fs.readFileSync(file))};}};walk(base);return result;}
const manifest={builds:Object.fromEntries(Object.entries(roots).map(([name,base])=>[name,{revision:config.builds[name].revision||null,files:assetManifest(base)}])),fixtureFiles:Object.fromEntries(['bundle.json','history-record.json','startup-projection.json','parent.json','projection-summary.json'].map(name=>[name,{bytes:fs.statSync(fixture+'/'+name).size,sha256:sha(fs.readFileSync(fixture+'/'+name))}])),runnerSha256:sha(fs.readFileSync(new URL(import.meta.url)))};
fs.writeFileSync(out+'/manifest.json',JSON.stringify(manifest,null,2));
const clientApi=[];
let activeBuild='historical',activeDevice='desktop',activeStage='seed',sourceUnavailable=false;const api=[],blocked=[];
const server=http.createServer((req,res)=>{try{
 const url=new URL(req.url,'http://localhost'),pathname=decodeURIComponent(url.pathname);
 if(pathname==='/replay-api'){
 const u=new URL(url.searchParams.get('target')),pathname=u.pathname,method=req.method,at=Date.now(),build=activeBuild,device=activeDevice,stage=activeStage;
 const read=method==='GET'||/\/rpc\/(?:atlas_read_|atlas_bonus_workspace$|atlas_directory|atlas_get_)/.test(pathname);
 res.setHeader('content-type','application/json');res.setHeader('cache-control','no-store');
 if(!read){blocked.push({build,device,stage,method,path:pathname});res.statusCode=403;res.end(JSON.stringify({message:'Replay blocks all mutations'}));return;}
 if(sourceUnavailable&&(pathname.endsWith('/rpc/atlas_read_workspace_projection')||pathname.endsWith('/atlas_app_documents'))){api.push({build,device,stage,method,path:pathname,at,finishedAt:Date.now(),status:503,bytes:0});res.statusCode=503;res.end(JSON.stringify({message:'Synthetic source unavailable'}));return;}
 let value=[];
 if(pathname.endsWith('/atlas_user_profiles'))value=[profile];
 if(pathname.endsWith('/atlas_communities'))value=roster;
 if(pathname.endsWith('/auth/v1/user'))value={id:actor,email:profile.email};
 if(pathname.endsWith('/rpc/atlas_read_workspace_projection'))value={status:'available',source:projection.source,projection,...binding};
 if(pathname.endsWith('/atlas_app_documents')){const key=u.searchParams.get('document_key')?.slice(3);if(key==='atlas_dashboard_state_v1')value=[{document_key:key,version:parent.version,updated_at:projection.source.effectiveAt,payload:{bundle:parent.archive}}];}
 if(pathname.endsWith('/rpc/atlas_bonus_workspace'))value={permissions:{},communities:[],employees:[],plans:[],runs:[]};
 const body=JSON.stringify(value);api.push({build,device,stage,method,path:pathname,at,finishedAt:Date.now(),durationMs:Date.now()-at,bytes:Buffer.byteLength(body),status:200});res.end(body);return;
 }
 if(pathname==='/replay-xlsx.js'){res.setHeader('content-type','text/javascript');res.setHeader('cache-control','public,max-age=3600');res.end(fs.readFileSync((config.xlsxAsset?path.resolve(config.xlsxAsset):roots.repaired+'/assets/xlsx.full.min.js')));return;}
 res.setHeader('content-security-policy',"default-src 'self' data: blob:; script-src 'self' 'unsafe-inline' 'unsafe-eval' blob:; style-src 'self' 'unsafe-inline'; connect-src 'self'; img-src 'self' data: blob:; font-src 'self' data:; frame-src 'self' data: blob:; worker-src 'self' blob:");
 if(pathname==='/seed.html'){res.setHeader('content-type','text/html');res.end('<!doctype html><title>Isolated seed</title>');return;}
 if(pathname==='/fixture/bundle.json'||pathname==='/fixture/history-record.json'){res.setHeader('content-type','application/json');fs.createReadStream(fixture+'/'+path.basename(pathname)).pipe(res);return;}
 const file=path.resolve(roots[activeBuild],'.'+pathname);if(!file.startsWith(roots[activeBuild]+path.sep))throw Error('scope');
 if(!file.endsWith('.html'))res.setHeader('cache-control','public, max-age=3600');
 res.setHeader('content-type',file.endsWith('.html')?'text/html':/\.(mjs|js)$/.test(file)?'text/javascript':file.endsWith('.css')?'text/css':'application/octet-stream');if(file.endsWith('.html')){res.end(fs.readFileSync(file,'utf8').replace(/https:\/\/[^\"'<>\s]*xlsx\.full\.min\.js/g,'/replay-xlsx.js'));}else fs.createReadStream(file).on('error',()=>{res.statusCode=404;res.end();}).pipe(res);
}catch{res.statusCode=404;res.end();}});
await new Promise(resolve=>server.listen(0,'127.0.0.1',resolve));const origin='http://127.0.0.1:'+server.address().port;
const browser=await chromium.launch({headless:true,args:['--enable-precise-memory-info']}),results=[];
const buildList=config.buildOrder||['historical','baseline','repaired'],devices=config.devices||['desktop','mobile'],samples=Number(config.samples??5),cycles=Number(config.cycles??10),settleMs=Number(config.settleMs??10000);
if(!Number.isInteger(samples)||samples<1||!Number.isInteger(cycles)||cycles<0)throw Error('Invalid sample/cycle count');
try{for(const build of buildList)for(const device of devices){
 activeBuild=build;activeDevice=device;activeStage='seed';const mobile=device==='mobile';const context=await browser.newContext({viewport:mobile?{width:390,height:844}:{width:1440,height:900},deviceScaleFactor:mobile?3:1,isMobile:mobile,hasTouch:mobile});
 // Keep real HTTP caching available. A same-origin transport replaces only API
 // fetches; CSP rejects every external connection and external asset in all builds.
 await context.addInitScript(()=>{
  const nativeFetch=window.fetch.bind(window);
  window.fetch=async(input,options)=>{
   const request=new Request(input,options),url=new URL(request.url);
   if(url.origin===location.origin||!/^https?:$/.test(url.protocol))return nativeFetch(request);
   if(url.pathname.startsWith('/rest/v1/')||url.pathname.startsWith('/auth/v1/'))return nativeFetch('/replay-api?target='+encodeURIComponent(url.href),{method:request.method,headers:request.headers,body:['GET','HEAD'].includes(request.method)?undefined:await request.text(),signal:request.signal});
   return new Response('',{status:200});
  };
 });
 const page=await context.newPage(),cdp=await context.newCDPSession(page),errors=[];
 const apiByRequest=new Map();let externalResponses=0;
 cdp.on('Network.requestWillBeSent',event=>{try{const url=new URL(event.request.url);if(url.pathname!=='/replay-api')return;const target=new URL(url.searchParams.get('target'));const row={build,device,stage:activeStage,method:event.request.method,path:target.pathname.replace(/[0-9a-f]{8}-(?:[0-9a-f]{4}-){3}[0-9a-f]{12}/ig,'[id]'),startedAt:event.timestamp};apiByRequest.set(event.requestId,row);clientApi.push(row);}catch{}});
 cdp.on('Network.responseReceived',event=>{const row=apiByRequest.get(event.requestId);if(row){row.status=event.response.status;row.responseAt=event.timestamp;}try{if(/^https?:/.test(event.response.url)&&new URL(event.response.url).origin!==origin)externalResponses++;}catch{}});
 cdp.on('Network.loadingFinished',event=>{const row=apiByRequest.get(event.requestId);if(row){row.finishedAt=event.timestamp;row.durationMs=(event.timestamp-row.startedAt)*1000;row.encodedBytes=event.encodedDataLength;row.outcome='completed';}});
 cdp.on('Network.loadingFailed',event=>{const row=apiByRequest.get(event.requestId);if(row){row.finishedAt=event.timestamp;row.durationMs=(event.timestamp-row.startedAt)*1000;row.outcome=event.canceled?'cancelled':'failed';row.error=event.errorText;}});let cacheHits=0,requestsStarted=0,requestsFailed=0;await cdp.send('Network.enable');cdp.on('Network.requestServedFromCache',()=>cacheHits++);page.on('request',()=>requestsStarted++);page.on('requestfailed',()=>requestsFailed++);
 page.on('pageerror',error=>errors.push(error.message.slice(0,180)));if(mobile)await cdp.send('Emulation.setCPUThrottlingRate',{rate:4});
 await page.addInitScript(()=>{window.__replayLong=[];try{new PerformanceObserver(list=>{for(const entry of list.getEntries())__replayLong.push({start:entry.startTime,duration:entry.duration});}).observe({type:'longtask',buffered:true});}catch{}});
 await page.goto(origin+'/seed.html');
 await page.evaluate(async({profile,namespace,projection,binding,repaired,syntheticAuth,backend})=>{
  const bundle=await(await fetch('/fixture/bundle.json')).json();const dbName=repaired?namespace:'atlas_rise_state_v1';
  const open=indexedDB.open(dbName,1);open.onupgradeneeded=()=>open.result.createObjectStore('records',{keyPath:'key'});const db=await new Promise((resolve,reject)=>{open.onsuccess=()=>resolve(open.result);open.onerror=()=>reject(open.error);});
  const records=[{key:'community_data',value:bundle.indexedDb.communityData},{key:'rise_ops_global_v1',value:JSON.parse(bundle.keys.rise_ops_global_v1)}];
  if(repaired){records.push({key:'atlas_workspace_source_v2',value:{identity:projection.source,binding,contentHash:projection.contentHash,verifiedAt:new Date().toISOString(),dirty:false}},{key:'atlas_startup_import_projection_v2',value:projection.importState});}
  else records.push(await(await fetch('/fixture/history-record.json')).json());
  await new Promise((resolve,reject)=>{const tx=db.transaction('records','readwrite');for(const record of records)tx.objectStore('records').put(record);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);});db.close();
  for(const key of ['rise_ops_workspace_context_v1','rise_ops_property_catalog_v1','atlas_shared_property_graph_v1'])if(bundle.keys[key])localStorage.setItem(key,typeof bundle.keys[key]==='string'?bundle.keys[key]:JSON.stringify(bundle.keys[key]));
  localStorage.setItem('atlas_central_runtime_config_v1',JSON.stringify({supabaseUrl:backend,supabaseAnonKey:syntheticAuth.accessToken,accessApiBaseUrl:backend,appBaseUrl:location.origin,autosave:false,realtime:false,autoPullOnStartup:false}));
  localStorage.setItem('atlas_central_auth_session_v1',JSON.stringify({access_token:syntheticAuth.accessToken,refresh_token:syntheticAuth.refreshToken,expires_at:Math.floor(Date.now()/1000)+86400,user:{id:profile.user_id,email:profile.email}}));localStorage.setItem('atlas_central_profile_v1',JSON.stringify(profile));
  const prefs=JSON.stringify({hasSeenWelcome:true});localStorage.setItem('atlas_dashboard_preferences_v1',prefs);localStorage.setItem(namespace+':atlas_dashboard_preferences_v1',prefs);
 },{profile,namespace,projection,binding,repaired:build==='repaired',syntheticAuth,backend});
 await cdp.send('HeapProfiler.collectGarbage');
 const group={kind:'REPLAY',build,device,source:summary.parentVersion,sourceHash:summary.archiveSha256,htmlHash:sha(fs.readFileSync(roots[build]+'/index.html')),reportsHash:fs.existsSync(roots[build]+'/features/reports-workspace.js')?sha(fs.readFileSync(roots[build]+'/features/reports-workspace.js')):null,coreHash:fs.existsSync(roots[build]+'/workspace-core.js')?sha(fs.readFileSync(roots[build]+'/workspace-core.js')):null,startup:[],navigation:[],errors};
 results.push(group);console.log('READY',build,device);
 const ready=()=>page.waitForFunction(()=>typeof atlasDashboardInitializationComplete!=='undefined'&&atlasDashboardInitializationComplete&&(typeof atlasWorkspaceAccess==='undefined'||atlasWorkspaceAccess.hasData),null,{timeout:120000});
 for(const cache of ['cold','warm'])for(let i=0;i<samples;i++){
  activeStage='startup-'+cache+'-'+(i+1);
  await cdp.send('Network.setCacheDisabled',{cacheDisabled:cache==='cold'});if(config.startupProfile){await cdp.send('Profiler.enable');await cdp.send('Profiler.start');}const start=Date.now(),firstApi=api.length,firstCacheHit=cacheHits,firstStarted=requestsStarted,firstFailed=requestsFailed;
  await page.goto(origin+'/index.html?atlasPerf=1',{waitUntil:'domcontentloaded',timeout:120000});await ready();await page.evaluate(()=>new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve))));const readyElapsed=Date.now()-start;
  await page.waitForTimeout(settleMs);
  if(config.startupProfile){const cpu=await cdp.send('Profiler.stop');fs.writeFileSync(out+'/'+build+'-'+device+'-startup-'+cache+'-'+i+'.cpuprofile',JSON.stringify(cpu.profile));}
  const metrics=await page.evaluate(()=>({workspaceMs:performance.getEntriesByName('atlas:time-to-workspace').at(-1)?.duration??null,longTasks:__replayLong,longestTask:Math.max(0,...__replayLong.map(x=>x.duration)),blockingTime:__replayLong.reduce((n,x)=>n+Math.max(0,x.duration-50),0),renderCount:window.AtlasPerformance?.report?.().counters?.render??null,heap:performance.memory?.usedJSHeapSize??null,requestCount:performance.getEntriesByType('resource').length,cachedResourceCount:performance.getEntriesByType('resource').filter(x=>x.transferSize===0&&x.decodedBodySize>0).length,resourceTransferBytes:performance.getEntriesByType('resource').reduce((n,x)=>n+x.transferSize,0),communities:Object.keys(savedData).length,batches:dataImport2State.batches.length,sourceVersion:typeof atlasWorkspaceAccess==='undefined'?null:atlasWorkspaceAccess.source?.version,paint:performance.getEntriesByType('paint').map(x=>({name:x.name,at:x.startTime})),renderDurations:window.AtlasPerformance?.report?.().events?.filter(x=>x.name==='render').map(x=>x.duration)||[],scope:isPortfolioWorkspaceSelected()?'portfolio':'community'}));
  group.startup.push({cache,index:i+1,cdpCacheHits:cacheHits-firstCacheHit,requestsStarted:requestsStarted-firstStarted,requestsFailed:requestsFailed-firstFailed,readyElapsed,observationMs:Date.now()-start,apiRequests:api.length-firstApi,...metrics});fs.writeFileSync(out+'/results.partial.json',JSON.stringify({results,api,clientApi,blocked},null,2));console.log('START',build,device,cache,i+1,readyElapsed,metrics.longestTask);
 }
 await cdp.send('HeapProfiler.collectGarbage');let memoryBefore=await cdp.send('Runtime.getHeapUsage'),domBefore=await cdp.send('Memory.getDOMCounters');
 for(let cycle=0;cycle<cycles+1;cycle++)for(const tab of [0,8,7,2,9]){
  activeStage='navigation-'+cycle+'-'+tab;
  const profileNavigation=Boolean(config.profileNavigation)&&tab===Number(config.profileTab??8); if(profileNavigation){await cdp.send('Profiler.enable');await cdp.send('Profiler.start');} const start=Date.now(),firstApi=api.length;const before=await page.evaluate(()=>({renders:window.AtlasPerformance?.report?.().counters?.render||0,longTaskCount:__replayLong.length,eventCount:window.AtlasPerformance?.report?.().events?.length||0}));const renderBefore=before.renders;
  await page.evaluate(tab=>setTab(tab),tab);await page.waitForFunction(tab=>activeTab===tab&&!!document.getElementById('tab-panel-'+tab)?.innerText&&!document.getElementById('tab-panel-'+tab).innerText.includes('Loading this workspace'),tab,{timeout:30000});await page.evaluate(()=>new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve))));
  if(profileNavigation){const cpu=await cdp.send('Profiler.stop');fs.writeFileSync(out+'/'+build+'-'+device+'-tab'+tab+'-'+cycle+'.cpuprofile',JSON.stringify(cpu.profile));} const metrics=await page.evaluate(({tab,renderBefore,before})=>({longestTask:Math.max(0,...__replayLong.slice(before.longTaskCount).map(x=>x.duration)),blockingTime:__replayLong.slice(before.longTaskCount).reduce((n,x)=>n+Math.max(0,x.duration-50),0),renderDurations:window.AtlasPerformance?.report?.().events?.slice(before.eventCount).filter(x=>x.name==='render').map(x=>x.duration)||[],renders:window.AtlasPerformance?.report?.().counters?.render==null?null:window.AtlasPerformance.report().counters.render-renderBefore,heap:performance.memory?.usedJSHeapSize??null,renderIssue:/render issue|did not finish loading/.test(document.getElementById('tab-panel-'+tab).innerText)}),{tab,renderBefore,before});group.navigation.push({stage:cycle===0?'first-loop':'repeated',cycle,tab,ms:Date.now()-start,apiRequests:api.length-firstApi,...metrics});console.log('NAV',build,device,cycle+1,tab,group.navigation.at(-1).ms);if(config.debugCache&&build==='repaired'&&tab===8){const cache=await page.evaluate(()=>({inputs:atlasCommunityProgressPreview?.inputs,counters:AtlasPerformance.report().counters,events:AtlasPerformance.report().events.filter(x=>x.name.includes('report-preview-cache'))}));fs.writeFileSync(out+'/cache-'+cycle+'.json',JSON.stringify(cache));}if(cycle===0&&tab===9){await cdp.send('HeapProfiler.collectGarbage');memoryBefore=await cdp.send('Runtime.getHeapUsage');domBefore=await cdp.send('Memory.getDOMCounters');}
 }
 await cdp.send('HeapProfiler.collectGarbage');group.heap={before:memoryBefore,domBefore,after:await cdp.send('Runtime.getHeapUsage'),dom:await cdp.send('Memory.getDOMCounters')};
 fs.writeFileSync(out+'/results.partial.json',JSON.stringify({results,api,clientApi,blocked},null,2));
 activeStage='parity';group.parity=await page.evaluate(async({legacyNormalizer,reportYear,reportMonth})=>{
  const hash=async value=>[...new Uint8Array(await crypto.subtle.digest('SHA-256',new TextEncoder().encode(JSON.stringify(value))))].map(x=>x.toString(16).padStart(2,'0')).join('');
  const homeInstance={widgetKey:'portfolio_overview',metric:'Physical Occupancy',scope:{type:'all_properties'}},homeSnapshot=buildAtlasDashboardWidgetSnapshot(homeInstance),homeHistory=window.AtlasReskin?.history?window.AtlasReskin.history(homeInstance,homeSnapshot):null;const originalNormalize=(0,eval)('('+legacyNormalizer+')'); let monthlyParity=true;for(const [name,record] of Object.entries(savedData)){for(const year of [2025,2026]){const normalized=originalNormalize(name,record);const original=Array.from({length:12},(_,month)=>({...defaultMonthly()[month],...getRecordMonthEntryForPeriod(normalized,month,year)}));if(JSON.stringify(original)!==JSON.stringify(getRecordMonthlyDataForYear(record,year)))monthlyParity=false;}} reportHubYear=reportYear;reportHubMonth=reportMonth;reportHubType='community_progress';const community=buildCommunityProgressReportData({silent:true});const communityStable=JSON.parse(JSON.stringify(community,(key,value)=>['generatedAt','sourceRecord'].includes(key)?undefined:value));if(community){community.generatedAt=new Date('2026-09-24T12:00:00Z');for(const row of community.communityReports||[])row.generatedAt=community.generatedAt;}const communityDocument=buildCommunityProgressReportDocument(community);reportHubType='portfolio_performance_report';const report=buildPortfolioReportData(),payload=buildPortfolioReportRows({silent:true});
  const values=Object.entries(savedData).sort(([a],[b])=>a.localeCompare(b)).map(([name,row])=>({name,monthlyData:row.monthlyData,monthlyHistoryByPeriod:row.monthlyHistoryByPeriod,currentOccupied:row.currentOccupied,currentLeased:row.currentLeased,customUnits:row.customUnits,financialLedger:row.financialLedger,financialBudgetLedger:row.financialBudgetLedger}));
  return {homeHistoryUnavailableReason:homeHistory===null?'This historical build predates AtlasReskin.history':null,diagnosticCounters:window.AtlasPerformance?.report?.().counters??null,homeHistoryHash:homeHistory===null?null:await hash(homeHistory),homeValue:homeSnapshot.value,monthlyNormalizationMatchesBaseline:monthlyParity,communityProgressHash:await hash(communityStable),communityDocumentHash:await hash(communityDocument),operatingDataHash:await hash(values),reportAggregateHash:await hash(report.aggregate),exportRowsHash:await hash(payload?.rows),reportDetailCount:report.details.length,exportRowCount:payload?.rows?.length??0,period:{year:report.reportYear,month:report.currentMonthIdx}};
 },{legacyNormalizer:fs.readFileSync(roots.baseline+'/index.html','utf8').match(/^function normalizeSavedCommunityRecord\([^]*?^\}/m)[0],reportYear,reportMonth});
 if(config.variants){group.variants=[];for(const scenario of ['slow','unavailable']){
  activeStage='variant-'+scenario;sourceUnavailable=scenario==='unavailable';
  await cdp.send('Network.setCacheDisabled',{cacheDisabled:false});
  await cdp.send('Network.emulateNetworkConditions',{offline:false,latency:scenario==='slow'?150:0,downloadThroughput:scenario==='slow'?200000:-1,uploadThroughput:scenario==='slow'?93750:-1,connectionType:scenario==='slow'?'cellular3g':'wifi'});
  const start=Date.now(),firstApi=api.length;await page.goto(origin+'/index.html?atlasPerf=1',{waitUntil:'domcontentloaded',timeout:120000});await ready();await page.evaluate(()=>new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve))));const readyElapsed=Date.now()-start;
  await page.waitForTimeout(10000);const detail=await page.evaluate(()=>({hasData:typeof atlasWorkspaceAccess==='undefined'?null:atlasWorkspaceAccess.hasData,validated:typeof atlasWorkspaceAccess==='undefined'?null:atlasWorkspaceAccess.validated,cached:typeof atlasWorkspaceAccess==='undefined'?null:atlasWorkspaceAccess.source?.cached,error:typeof atlasWorkspaceAccess==='undefined'?null:atlasWorkspaceAccess.error,longTasks:__replayLong,longestTask:Math.max(0,...__replayLong.map(x=>x.duration)),blockingTime:__replayLong.reduce((n,x)=>n+Math.max(0,x.duration-50),0),value:buildAtlasDashboardWidgetSnapshot({widgetKey:'portfolio_overview',metric:'Physical Occupancy',scope:{type:'all_properties'}}).value}));
  group.variants.push({scenario,readyElapsed,observationMs:Date.now()-start,apiRequests:api.length-firstApi,...detail});console.log('VARIANT',build,device,scenario,readyElapsed,detail.value);
 }
 sourceUnavailable=false;await cdp.send('Network.emulateNetworkConditions',{offline:false,latency:0,downloadThroughput:-1,uploadThroughput:-1,connectionType:'wifi'});
 }
 if(config.debugParity)fs.writeFileSync(out+'/'+build+'-community-report.json',JSON.stringify(await page.evaluate(()=>{reportHubType='community_progress';return JSON.parse(JSON.stringify(buildCommunityProgressReportData({silent:true}),(key,value)=>key==='generatedAt'?undefined:value));})));
 fs.writeFileSync(out+'/results.partial.json',JSON.stringify({results,api,clientApi,blocked},null,2));group.externalResponseCount=externalResponses;if(externalResponses)throw Error('Replay isolation failed: external network response observed');await context.close();
}
 const final={kind:'READ-ONLY ISOLATED REPLAY',browser:browser.version(),platform:process.platform,architecture:process.arch,hardware:{cpu:os.cpus()[0]?.model,cpuCount:os.cpus().length,totalMemoryBytes:os.totalmem()},date:new Date().toISOString(),fixture:{...summary,privatePathsOmitted:true},configuration:{buildOrder:buildList,buildRevisions:Object.fromEntries(Object.entries(config.builds).map(([name,value])=>[name,value.revision||null])),reportingPeriod:period,samples,cycles,firstLoopAlsoRecorded:true,settleMs,devices,slowNetwork:config.variants?'150 ms RTT;200,000 B/s down;93,750 B/s up;cached assets':null,desktop:'1440x900 DPR1 no throttle',mobile:'390x844 DPR3 touch 4xCPU',externalAssets:'CSP blocks all external network; API fetches use a same-origin synthetic transport, pinned XLSX served locally, external fonts/map dependencies unavailable'},limits:['Not independently authenticated production acceptance','Historical 4263a58 predates AtlasReskin history and performance diagnostics; those unavailable probes are null, not fabricated. Home-history parity compares baseline and repaired only.','Old builds use full retained177MB history; repaired uses its exact derived current projection while full immutable history remains available in local fixture','Canonical archive13batches differs from original Chrome29batch state','HTTP cache disabled for cold and enabled for warm; verified cached-resource counts retained. Startup and navigation have no artificial network latency; separate slow variants use the recorded network profile. Heap growth is measured after the first complete module-loading loop, then ten repeated loops. Main-page CDP heap excludes workers and browser processes. CDP CPU slowdown applies to the page, not workers; background freshness may remain pending after the ten-second slow-source observation.', 'Community report data hash omits internal sourceRecord and generatedAt; exact rendered HTML hash fixes generation clock. Baseline overwrites one canonical manager identity with a local ID, repaired preserves canonical ID; no visible report field differs.', 'Metrics use isolated reads and blocked mutation endpoints; denied writes may alter old-build fallback work'],results,api,clientApi,blocked};
 fs.writeFileSync(out+'/results.json',JSON.stringify(final,null,2));console.log('COMPLETE',results.length,'groups; blocked writes',blocked.length);
}finally{await browser.close();await new Promise(resolve=>server.close(resolve));}
