const assert=require('node:assert/strict'),fs=require('node:fs'),vm=require('node:vm');
const W=require('../docs/portfolio-operations-dashboard/weekly-leasing-report.js');
const html=fs.readFileSync(__dirname+'/../docs/portfolio-operations-dashboard/index.html','utf8');
const functions=[...html.matchAll(/^(?:async )?function [A-Za-z_$][\w$]*\([^\n]*\) \{[\s\S]*?^\}/gm)].map(x=>x[0]);
const extract=name=>functions.find(x=>x.startsWith(`function ${name}(`));
const base={property:'Active',leasingAgent:'Agent <A>',newLeadCreatedOn:'2026-09-11',applicationCompleted:'2026-09-12',sourceAsOf:'2026-09-16',applicationStatus:'Application: Completed',lifecycleCoverage:'unavailable'};
const data=W.model({records:[base,{...base,property:'Inactive'},{...base,newLeadCreatedOn:'2026-08-01',applicationCompleted:'',applicationStatus:'Application: Started'},{...base,newLeadCreatedOn:'2026-09-18',applicationCompleted:'2026-09-18'}],communities:['Active'],period:'2026-09',end:'2026-09-17',metrics:[{community:'Active',cells:{applications:{value:0,label:'Applications',source:'Verified zero'}}}],offers:[]});
assert.equal(data.start,'2026-09-11');assert.equal(data.totals.leads,1);assert.equal(data.totals.completed,1);assert.equal(data.totals.overdue,1);assert.equal(data.agents[0].approvals,null);assert.equal(data.communityRows.length,1);
assert(W.document(data).includes('Agent &lt;A&gt;'));assert(!W.document(data).includes('Inactive'));assert(W.rows(data).some(r=>r.value==='0'&&r.note==='Verified zero'));assert.equal(W.model({records:[base],communities:[],period:'2026-09',end:'2026-09-17'}).totals.records,0);
const context={Date,Math,Number,FULL_MONTHS:['Jan'],DATA_IMPORT_REPORT_ORDER:['resident'],dataImport2State:{exceptions:[],batches:[]},dataImportGetHealthCommunityNames:()=>['Active'],dataImportGetMatrixCommunityNames:()=>['Active','Inactive'],dataImportIsActiveReportingCommunity:n=>n==='Active',dataImportGetReportDef:()=>({label:'Residents'}),dataImportEffectiveFreshness:()=>({days:7}),dataImportCommunitySupportsReport:()=>true,dataImportLatestArchiveFor:()=>null,dataImportHasSavedFallbackData:()=>true,dataImportNormalizeText:s=>s,dataImportFormatDate:s=>s};
vm.createContext(context);vm.runInContext(extract('dataImportGetRequirementOverride')+'\n'+extract('dataImportBuildStatusDateLabel')+'\n'+extract('dataImportBuildHealthModel'),context);
let health=context.dataImportBuildHealthModel();assert.equal(health.rows[1].cells[0].status,'Historical');assert.equal(health.rows[1].cells[0].required,false);assert.equal(health.rows[0].cells[0].status,'Saved');assert(!health.rows[1].cells[0].statusDateLabel.includes('January'));
context.dataImportLatestArchiveFor=()=>({batchId:'test',dataDateIso:'2026-09-16'});context.dataImportAgeForArchive=()=>1;health=context.dataImportBuildHealthModel();assert.equal(health.rows[0].cells[0].status,'Fresh');assert.equal(health.rows[1].cells[0].status,'Historical');
vm.runInContext(extract('getTopLeadSourceFacts'),context);
const leadFacts=context.getTopLeadSourceFacts({walkIn:2,phoneCalls:7,emailsOnline:4,applicationApprovalPct:50,tourToAppPct:46.1538,applications:20});
assert.equal(leadFacts.length,3);assert.equal(leadFacts[0].key,'phoneCalls');assert.equal(leadFacts[0].value,7);assert(!leadFacts.some(item=>/Pct$/.test(item.key)||item.key==='applications'));
// Exercise actual settings persistence with shared rows, and ensure they never enter storage.
const shared={centralImportId:'authorized-session',records:[base]},local={records:[]};
const ctx={applicationResidentDataState:{uploads:[shared,local]},OPS_GLOBAL_STORAGE_KEY:'settings',serializeJacsTeamBonus:x=>x,persistOpsGlobalSnapshot:(data)=>{assert(!data.applicationResidentData.uploads.some(u=>u.centralImportId));return {ok:true};}};
const persist=extract('persistOpsGlobalData');const names=['atlasInvestorPacketState','bonusQuarter','bonusQuarterManualOverride','bonusCalcView','bonusEngineState','jacsTeamBonus','regionalAssignments','salarySecurity','monthlyPresentationCommunities','reportHubCommunityProgressCommunities','reportHubLvrCommunity','weeklyExecutiveEmail','portfolioReportSections','portfolioReportFilters','portfolioReportMode','atlasWorkbookImportMode','portfolioMonthScopeByPeriod','reportHubType','reportHubPerspective','reportHubMonth','reportHubYear','reportRecommendationOverrides','communityCommandState','windowshadeState','atlasSharedData'];names.forEach(n=>ctx[n]={});ctx.applyOpsGlobalData=d=>{ctx.applicationResidentDataState=d.applicationResidentData;ctx.opsGlobalData=d;};vm.createContext(ctx);vm.runInContext(persist,ctx);ctx.persistOpsGlobalData();assert.equal(ctx.applicationResidentDataState.uploads[0],shared);
// Parse every inline script, including generated template content within JS strings.
for(const match of html.matchAll(/<script\b[^>]*>([\s\S]*?)<\/script>/gi)){if(match[1].trim())new vm.Script(match[1]);}
console.log('PASS weekly boundaries, empty authorization, missing-versus-zero, escaped output, inactive history, settings preserve shared records, inline syntax.');
